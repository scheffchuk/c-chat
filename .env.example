# Convex: run `npx convex dev` to auto-populate; or from dashboard.convex.dev
NEXT_PUBLIC_CONVEX_URL=
CONVEX_DEPLOYMENT=
NEXT_PUBLIC_CONVEX_SITE_URL=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# AI Gateway: vercel.com → AI Gateway → API Keys
AI_GATEWAY_API_KEY=

# Resend: verified sender for email OTP (resend.com)
AUTH_EMAIL_FROM=

# Optional: Upstash Redis for resumable streaming
# REDIS_URL=
