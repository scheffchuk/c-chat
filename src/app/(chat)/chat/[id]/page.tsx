export default async function Page({ params }: { params: { id: string } }) {
  return (
    <div className="flex h-screen items-center justify-center font-bold font-sans text-4xl">
      Chat goes here
    </div>
  );
}
