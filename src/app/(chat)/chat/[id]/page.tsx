import React from "react";

export default async function Page({ params }: { params: { id: string } }) {
  return <div className="flex font-sans items-center justify-center h-screen text-4xl font-bold">Chat goes here</div>;
}
